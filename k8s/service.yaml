apiVersion: v1
kind: Service
metadata:
  name: getpixii-service
  namespace: getpixii
  labels:
    app: getpixii
spec:
  type: LoadBalancer
  loadBalancerIP: 10.50.0.52
  selector:
    app: getpixii
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
