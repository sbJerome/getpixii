# Example only — the real secret is created out-of-band (never committed).
# kubectl -n getpixii create secret generic getpixii-db \
#   --from-literal=DATABASE_URL='postgres://k3s:<PASSWORD>@postgres.services.svc.cluster.local:5432/getpixii'
apiVersion: v1
kind: Secret
metadata:
  name: getpixii-db
  namespace: getpixii
type: Opaque
stringData:
  DATABASE_URL: "postgres://k3s:CHANGEME@postgres.services.svc.cluster.local:5432/getpixii"
